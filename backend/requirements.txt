# ISRO Lunar Ice Explorer — Backend Dependencies
# Pinned for reproducible builds on Render (Python 3.11, Ubuntu x86-64)

fastapi==0.115.0
uvicorn[standard]==0.30.0
numpy==1.26.4
scipy==1.13.0
scikit-image==0.23.2
matplotlib==3.9.0
pydantic==2.8.0
python-multipart==0.0.9
aiofiles==24.1.0
httpx==0.27.0
pillow==10.4.0

# ── Real-data geospatial I/O (OPTIONAL — local only) ─────────────────────────
# rasterio needs libgdal system libraries not available on Render's free tier.
# The app works fully without it (LOLA DEM is loaded from the pre-built .npy
# file; real DFSAR GeoTIFFs can be processed locally by installing rasterio).
#   pip install rasterio==1.3.10
